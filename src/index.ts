/**
 * The package's one export at spin-up — a stub that proves the toolchain end to end
 * (build, declarations, test, and the CLI). Replace it with the real API.
 */
export function hello(name: string): string {
  return `Hello, ${name}!`;
}
