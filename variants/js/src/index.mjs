/**
 * The package's one export at spin-up — a stub that proves the toolchain end to end
 * (typecheck via checkJs, test, and the CLI). Replace it with the real API.
 *
 * @param {string} name
 * @returns {string}
 */
export function hello(name) {
  return `Hello, ${name}!`;
}
